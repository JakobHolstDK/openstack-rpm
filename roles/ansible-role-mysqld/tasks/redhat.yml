
- name: redhat | Install tools on target servers
  ansible.builtin.package:
    name:  '{{ item  }}'
    state: present
  with_items:
    - "socat"
    - "logrotate"
    - "wget"
    - "python3"
  become: True

#- name: Add repository for mysql
#  yum_repository:
#    name: mysql
#    description: mysql repo
#    baseurl: https://repo.mysql.com/yum/
#  become: True

  

- name: Install mysqld from the right source
  yum:
    name:  [ "mysql-server:i8.0.21-1.module+el8.2.0+7855+47abd494" ]
    disablerepo: "*"
    enablerepo:
      - rhel-8-for-x86_64-appstream-rpms

